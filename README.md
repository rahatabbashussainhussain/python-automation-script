 Python Data Automation Script

A simple Python project that automates repetitive data entry and formatting tasks using CSV files.

 Overview
Reads names and emails from `input.csv` and generates a clean `output.csv` with formatted data.

 How to Run
1. Make sure both files are in the same folder.
2. Run:
   ```bash
   python data_automation.py
